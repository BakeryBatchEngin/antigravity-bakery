import os
with open('src/app/admin/doughs/page.tsx', 'r', encoding='utf-8') as f:
    original = f.read()

# I will write a new version based on original and overwrite it.
# Actually, I should use regex to replace specific parts carefully.
