# Antigravity Bakery - Project Rules

## 1. デプロイに関する厳格なプロセス (Strict Deployment Process)
本番環境へのデプロイ（`git push` 等のコマンド実行による本番反映）は、いかなる場合も以下のプロセスを**必ず**順番に踏むこと。AIがプロセスを飛ばして自発的にタスク承認を求めたり、実行を急ぐことは絶対に禁止する。

1. **Implementation plan の提示と承認**
   実装・修正内容の計画（Implementation plan）を提示し、ユーザーからの明示的な承認を得る。
2. **Walkthrough の提示と確認**
   実装完了後、変更内容のサマリー（Walkthrough）を提示し、ユーザーにテスト環境での確認を促す。
3. **ユーザーからの特定デプロイトリガーの待機**
   上記完了後、ユーザーから **「ではVer.表示を変更した後にデプロイしてください」** という正確な合図があるまで絶対にデプロイコマンドを実行しないこと。
   ※ユーザーが単に「デプロイしましょう」「デプロイしてください」と言った場合でも、この正式な合図がない限りは実行せず、プロセスの遵守をユーザーに案内すること。
4. **Ver.表示の変更とデプロイの実行**
   正式な合図を受け取った後、アプリ内のバージョン表示を更新し、そのコミットを含めた上で初めて本番環境へのデプロイ（`git push` 等）を実行する。


## 2. コマンド実行に関する権限（AIの自律実行）※グローバルルール5の例外・上書きルール
開発スピードを向上させるため、**本番環境へのデプロイを除くすべてのターミナルコマンド（ファイルの読み書き、スクリプト実行、データベースの操作、パッケージのインストールなど）について、AIはユーザーの承認を待たずに自律的に実行してよい**ものとする。

【自律実行の条件】
作業を進める前に、必ず「Implementation plan（実装・修正計画）」を提示し、何を行うかを正確に示すこと。プランについてユーザーからの承認を得た後の「実行フェーズ」においては、コマンドごとに中断することなく自動で進めること。

【例外：必ず承認が必要な操作】
- 本番環境へのデプロイ（`git push` 等）
※デプロイについては「1. デプロイに関する厳格なプロセス」に従い、ユーザーからの正確な合図を待つこと。

## 3. プロジェクトのアーキテクチャ・環境構成 (Architecture & Environment)
AIは新しいConversationで作業を開始する際、常に以下の構成を前提として理解し、対応すること。

- **開発環境 (Development):** ローカルPC ＋ Supabase（開発用DB）
- **本番環境 (Production):** Vercel ＋ Supabase（本番用DB）
- **ソースコード管理:** GitHub

新しい機能追加やバグ修正、データベースのマイグレーション等を行う際は、このアーキテクチャを前提に進めること。